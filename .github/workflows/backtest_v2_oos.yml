name: Backtest v2 OOS (2018-2022)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run v2 OOS backtest (2018-2022)
        run: python backtest_v2_oos.py 2>&1 | tee backtest_v2_oos_log.txt
      - name: Commit results
        if: success()
        run: |
          git config user.name  "github-actions"
          git config user.email "github-actions@github.com"
          for f in backtest_v2_oos.json backtest_v2_oos_log.txt; do
            [ -f "$f" ] && git add "$f" || true
          done
          git diff --staged --quiet || git commit -m "feat: v2 OOS backtest result"
          git push
